// Particle Lifespan
lifespan = irandom_range(0.10, 30); // number of steps before disappearing

// Set Random Motion
hsp = random_range(-0.5, 0.5); // horizontal speed (left-right)
vsp = random_range(-0.01, -0.5); // vertical "kick" upward

